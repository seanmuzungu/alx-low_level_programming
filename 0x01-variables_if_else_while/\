#include <stdio.h>

/**
 * main entry point
 * The main programm happens here
 * `everything else ma1
 * Return: Always retuns 0
 */

int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	putchar(a);
	putchar('\n');

	return (0);
}
